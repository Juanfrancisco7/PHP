<?php

$numero = NULL;

if (is_null($numero)){
    echo "Es nula";
} else {
    echo "No es nula";
}

/*<?php
// Supongamos que recibimos los datos del formulario
$nombre = $_POST['nombre'];
$correo = $_POST['correo'];
$telefono = $_POST['telefono'];

// Verificar si el campo de teléfono está definido y no es nulo
if (!is_null($telefono)) {
    echo "El usuario proporcionó un número de teléfono: $telefono";
} else {
    echo "El usuario no proporcionó un número de teléfono.";
}
?>
*/
